# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-multipart>=0.0.18

# HTTP + utilities
httpx>=0.27.0
python-dotenv>=1.0.1

# Vector + embeddings
numpy>=1.26.0
# sentence-transformers is large; only install when you need real embeddings.
# For the demo, we ship a keyword + TF-IDF fallback that needs nothing heavy.
# sentence-transformers>=3.3.0
# faiss-cpu>=1.9.0

# LLM clients (optional — app runs without keys via mock providers)
# openai>=1.54.0
# anthropic>=0.39.0

# Video ingestion (optional; app ships with pre-cached demo cases)
# yt-dlp>=2024.12.13

# ASR (optional; GPU-preferred; app ships with pre-cached transcripts)
# faster-whisper>=1.1.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
