"""External provider adapters.

Adapters in this package are optional and best-effort. The deterministic demo
paths remain the default behavior unless provider credentials and runtime
dependencies are configured.
"""

